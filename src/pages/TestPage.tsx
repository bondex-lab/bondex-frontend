import React from 'react';
import TestButton from "../components/TestButton";

const TestPage = () => {
    return (
        <div>
            <TestButton/>
        </div>
    );
};

export default TestPage;